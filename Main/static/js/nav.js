function OnBubbleClick() {
    var url = SgtTools.Instance().GetFullUrl('sort/bubble');
    window.location.href = url;
}
function OnSelectionClick() {
    var url = SgtTools.Instance().GetFullUrl('sort/selection');
    window.location.href = url;
}
function OnInsertionClick() {
    var url = SgtTools.Instance().GetFullUrl('sort/insertion');
    window.location.href = url;
}
function OnBinaryClick() {
    var url = SgtTools.Instance().GetFullUrl('search/binary');
    window.location.href = url;
}
function OnRecursionBinaryClick() {
    var url = SgtTools.Instance().GetFullUrl('search/binary_r');
    window.location.href = url;
}